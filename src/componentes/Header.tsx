function Header() {
    return(
        <header>
            <nav  className="navbar">
                <a href="#">Produtos</a>
                <a href="#">Cadastrar</a>
            </nav>
        </header>
    );
}

export default Header;